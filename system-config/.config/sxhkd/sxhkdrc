mod4 + {u,i,o}
  amixer set Master {playback 2-,toggle,playback 2+}
mod4 + shift + {u,o}
  amixer set Master playback 10{-,+}

mod4 + z
  slock

mod4 + p
  gmrun

mod4 + {minus,equal,BackSpace}
  mpc -h ~/.mpd/socket {toggle,next,prev}

mod4 + comma; {e,r}
  sudo {poweroff,reboot}

mod4 + comma; {h,t,g}
  i3-msg 'workspace 1'; {chromium,tor-browser-en,google-chrome-stable}

mod4 + comma; b
  pere random

mod4 + comma; f
  synclient touchpadoff=$(synclient -l | grep -c 'TouchpadOff.*=.*0')

mod4 + comma; l
  i3-msg 'workspace 8'; keepassx2 ~/passwords.kdbx

mod4 + comma; n
  i3-msg 'workspace 10'; gnome-terminal -e ncmpcpp

mod4 + comma; shift + n
  i3-msg 'workspace 10'; gnome-terminal -e 'mosh cdchawth@nullsleep.csclub.uwaterloo.ca -- screen -DR'

mod4 + comma; {_, shift +} d
  i3-msg 'workspace 2'; gnome-terminal -e {nvim,tmux}

mod4 + comma; c
  i3-msg 'workspace 7'; chromium --app-id=bikioccmkafdpakkkcpdbppfkghcmihk

mod4 + comma; s
  steam-native
