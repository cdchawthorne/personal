#!/usr/bin/env zsh

if [[ $# -ne 2 ]]; then
  echo "USAGE: $0 SPEAKERS_DEVICE HEADPHONE_DEVICE" >&2
  exit 1
fi

sink="$(pactl get-default-sink)"
if [[ $sink == $1 ]]; then
  echo 🔊
elif [[ $sink == $2 ]]; then
  echo 🎧
fi
